<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StudentOrderFood" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Áp dụng cho tất cả MaterialButton -->
        <item name="materialButtonStyle">@style/AppButtonStyle</item>
        <item name="fontFamily">@font/abeezee</item> <!-- Cho Material Components -->
        <item name="android:fontFamily">@font/abeezee</item>
        <!-- Hoặc cho tất cả Button thường -->
        <item name="buttonStyle">@style/AppButtonStyle</item>
    </style>

    <style name="Theme.StudentOrderFood" parent="Base.Theme.StudentOrderFood" />
</resources>